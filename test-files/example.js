// This is a single-line comment in JavaScript

const PI = 3.14; // Declaring a constant variable

/* 
   Multi-line comment:
   JavaScript does not enforce strict typing, 
   so variables can be reassigned to different types.
*/
let counter = 0;

function add(a, b) {
    // Function to add two numbers
    return a + b;
}

/*
   Class definition: MyClass represents a simple object with a counter.
   The increment method increases the counter.
*/
class MyClass {
    constructor() {
        this.value = 0;
    }

    increment() {
        this.value++;
    }
}
